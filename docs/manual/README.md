## manual

This folder stores the split static user manual.

- `assets/` contains shared CSS and JavaScript for the manual shell and section pages.
- `sections/` contains one HTML document per manual section.

`../manual.html` is the shell page with header, sidebar, and an iframe. Add or remove manual sections by updating both the sidebar link in `../manual.html` and the matching file in `sections/`.
