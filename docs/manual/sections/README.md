## sections

Each HTML file in this folder is one manual chapter.

Keep section files self-contained and linked to `../assets/manual.css`. The shell page loads these files in an iframe, so section pages should not define sidebar or global navigation.
